(function () {

    'use strict';

    var app = angular.module('app.raceState');

    app.controller('raceCtrl', raceCtrl);

    raceCtrl.$inject = [];

    function raceCtrl() {
        var vm = angular.extend(this, {});
        return vm;
    }
})();
