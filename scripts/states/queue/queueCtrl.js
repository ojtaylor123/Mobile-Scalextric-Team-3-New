(function () {

    'use strict';

    var app = angular.module('app.queueState');

    app.controller('queueCtrl', queueCtrl);

    queueCtrl.$inject = [];

    function queueCtrl() {
        var vm = angular.extend(this, {});
        return vm;
    }

})();
